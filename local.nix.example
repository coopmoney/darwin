{ ... }:

{
  # Machine-specific overrides
  # Copy this file to local.nix and customize for this machine
  # local.nix is gitignored and won't be committed
  
  # Override git user info for this machine
  # programs.git = {
  #   userName = "Your Name";
  #   userEmail = "your.email@example.com";
  # };
  
  # Add machine-specific packages
  # environment.systemPackages = with pkgs; [
  #   # machine-specific tools
  # ];
  
  # Machine-specific environment variables
  # environment.variables = {
  #   CUSTOM_VAR = "value";
  # };
}