# cd to development directory
alias dev='cd ~/Developer'

# chamber
alias lkr='aws-vault exec dmadmin -- chamber read locker'
alias lkw='aws-vault exec dmadmin -- chamber write locker'
alias lkl='aws-vault exec dmadmin -- chamber list locker'

alias curse='cursor editor'
